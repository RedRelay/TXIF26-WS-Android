Il faut node.js installé.

Pour installer les librairies externe : ouvrir node command line, se mettre dans le repertoire webservice-test avec 'cd' puis lancer :
npm install restify
npm install restify-errors

Pour lancer le webservice, ouvrir node command line, se mettre dans le repertoire webservice-test avec 'cd' puis lancer :
node webservice-test